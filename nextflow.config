process {
    executor = 'lsf'
    queue    = 'large_mem'

    withName: CELLSNP {
        cpus   = params.threads
        memory = params.memory
        time   = '24h'
    }
}

params {
    config = 'config.yml'
}
